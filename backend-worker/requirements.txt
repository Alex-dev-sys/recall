# Core dependencies
pyrogram==2.0.106
# tgcrypto==1.2.5  # Optional, requires C++ Build Tools on Windows
supabase>=2.10.0
pyyaml>=6.0.1

# AI providers
openai>=1.50.0
anthropic>=0.39.0

# Web framework
fastapi>=0.115.0
uvicorn>=0.30.0

# Utilities
python-dotenv>=1.0.1
cryptography>=42.0.0
aiohttp>=3.9.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Date parsing
python-dateutil>=2.8.2
dateparser>=1.2.0

# Logging and monitoring
loguru>=0.7.2

# Rate limiting
slowapi>=0.1.9

# Retry logic
tenacity>=8.2.0

# Async support
# asyncio is built into Python 3.7+, no need to install
uvloop>=0.19.0; sys_platform != 'win32'

# Database
asyncpg>=0.29.0

# Testing (optional)
pytest>=8.0.0
pytest-asyncio>=0.23.0
